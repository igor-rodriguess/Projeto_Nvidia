services:
  searxng:
    image: searxng/searxng:latest
    container_name: nvidia-ai-radar-searxng
    ports:
      - "8080:8080"
    environment:
      - SEARXNG_BASE_URL=http://localhost:8080/
    volumes:
      - searxng_data:/etc/searxng
    restart: unless-stopped

volumes:
  searxng_data:
